<div class="container">

<p id="notice"><%= notice %></p>

<h1>Escolha sua atração! </h1>

<div class="list-group">
  <div class="list-group-item active">
   <h3> Tipo de Atração </h3>
  </div>
  <%=link_to 'Ponto Turístico ', locals_path,  class:"list-group-item"%>
  <a href="#" class="list-group-item">Museus</a>
  <a href="#" class="list-group-item">Feiras e Mercados</a>
  <a href="#" class="list-group-item">Teatros</a>
</div>


<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

</div>


  <tbody >
    <% @buscas.each do |busca| %>
      <tr>
        <td><%= link_to 'Show', busca %></td>
        <td><%= link_to 'Edit', edit_busca_path(busca) %></td>
        <td><%= link_to 'Destroy', busca, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Busca', new_busca_path %>
